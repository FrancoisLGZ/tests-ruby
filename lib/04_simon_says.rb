def echo(a)
  return a
end

def shout(b)
  return b.upcase
end

def repeat(c,x)
  return (c * x) #Manque l'espace et est fixé à x (fonctionne donc pour l'exemple avec 3). Comment imprimer 2 hello sans mettre de x comme dans le test ??
end

def start_of_word(d,y)
  return d[0,y] #Formule sur internet + test à tâton.
end

def first_word(e)
  e.split.first #Internet
end

def titleize(f) #Compliqué + manque de temps

end